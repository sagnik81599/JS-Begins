// Synchronous code executes one line at a time. The next line waits until the current line finishes.
//Asynchronous code does not block the rest of the program. Long-running tasks are started, and JavaScript continues executing other code while waiting.

//javascript is a sync but later it was async by callback functions